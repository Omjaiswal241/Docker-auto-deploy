name: Deploy Backend to Docker Hub

on:
  push:
    branches: [ main ]
    paths:
      - 'todo-app/apps/backend/**'
      - 'todo-app/packages/db/**'
      - 'todo-app/docker/Dockerfile.backend'

jobs: 
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}
      
      - name: Build and push backend
        uses: docker/build-push-action@v5
        with:
          context: ./todo-app
          file: ./todo-app/docker/Dockerfile.backend
          push: true
          tags: |
            omjaiswal241/todo-app-backend:latest
            omjaiswal241/todo-app-backend:${{ github.sha }}
